# Field Pack 임시 보관 폴더

Windows 레이아웃 에디터에서 만든 `headunit-pi-field-pack.zip`을 Pi에서 적용하기 전에 이 폴더에 복사한다.

```bash
~/headunit-revived-esp32-hud/field-pack/headunit-pi-field-pack.zip
```

zip 파일은 로컬 배포물이므로 Git에 올리지 않는다.
