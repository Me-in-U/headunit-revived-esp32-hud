# Navigation icon assets

These PNG masks are generated from Google Material Design Icons SVG assets
(`@material-design-icons/svg`, Apache-2.0 license) by:

```powershell
cd layout-editor-electron
npm run generate-nav-icons
```

The Pi renderer tints the alpha mask at runtime, so the PNGs stay color-neutral.
